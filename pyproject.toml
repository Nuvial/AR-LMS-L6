[tool.pytest.ini_options]
testpaths = ["tests"]

[tool.coverage.run]
source = ["app", "db", "routes"]
omit = ["tests/*"]

[tool.coverage.report]
show_missing = true
skip_covered = false

[tool.bandit]
exclude_dirs = ["tests", ".venv"]
